import { describe, it } from "vitest";

// On-chain balance checks will be implemented with the OKX DEX integration.

describe("on-chain balance (pending OKX DEX integration)", () => {
  it.skip("tests pending OKX DEX on-chain balance implementation", () => {});
});
